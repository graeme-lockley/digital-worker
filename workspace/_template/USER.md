# User

## Operator (placeholder)

This file holds **agent-specific operator context** for the template deployment.

In a real deployment:

- Shared operator facts → wiki `people/<name>` via `wiki_write`
- Agent-specific notes → this file via `update_user`

Replace this placeholder when creating your own agent in `digital-worker-workspace`.
